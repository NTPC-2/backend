server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8

spring:
  application:
    name: Demo

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: javauser
    url: jdbc:mysql://127.0.0.1:3306/insuleng?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
    password: glory510

  jpa:
    properties:
      hibernate:
        format_sql: 'true'
    show-sql: 'true'
    hibernate:
      ddl-auto: update